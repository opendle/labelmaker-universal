# Server application

This directory is reserved for an optional headless API and print bridge. It
must use the same domain and printing contracts as the desktop application.

A remote container cannot directly use Bluetooth or USB attached to a user's
computer. That deployment needs a local agent near the printer.
